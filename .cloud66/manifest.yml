rails:
  configuration:
    ruby_version: "2.7.2"
mysql:
  groups:
    one:
      configuration:
        version: 8.0
      servers:
      - server:
          unique_name: one_server
    two:
      configuration:
        version: 8.0
      servers:
      - server:
          unique_name: two_server
